/*+----------------------------------------------------------------+
  |	Title:		FillMgr.hpp [共通環境]
  |	Comment:	塗り管理（形骸化）
  |	Author:		K.Takayanagi
  +----------------------------------------------------------------+*/
/*+----------------------------------------------------------------+
  |	Header Define	ヘッダ定義
  +----------------------------------------------------------------+*/
#ifndef __FILL_MGR_HPP__
#define __FILL_MGR_HPP__

/*+----------------------------------------------------------------+
  |	Include		インクルードヘッダ
  +----------------------------------------------------------------+*/
#include "env.hpp"
#include "Fill.hpp"

/*+----------------------------------------------------------------+
  |	Define		デファイン定義
  +----------------------------------------------------------------+*/
/*+----------------------------------------------------------------+
  |	Struct		構造体定義
  +----------------------------------------------------------------+*/
/*+----------------------------------------------------------------+
  |	Class		クラス定義
  +----------------------------------------------------------------+*/
//-----------------------------------------------------------------
// 塗り管理（※形骸化＝キャッシュ等での効率化を考えていたがその必要もなさそう）
//-----------------------------------------------------------------
class CFillMgr{
private:
    // work（※設定を反映して呼び出し元へ返すオブジェクト）
    static CFill s_oCurFill;

public:
    // 領域確保情報（※端末の解像度によって確保数とサイズが変動する）
    static int GetAllocCellNum( void );
    static int GetAllocSizeK( void );

	// 開始＆終了
    static bool OnCreate( void );
    static void OnDestroy( void );

    // 取得
    static CFill* GetFill( eFILL_TYPE type );

private:
	// インスタンス作成は不可
	CFillMgr( void ){}
	virtual ~CFillMgr( void ){}
};

/*+----------------------------------------------------------------+
  |	Global		グローバルデータ型定義
  +----------------------------------------------------------------+*/
/*+----------------------------------------------------------------+
  |	Prototype	プロトタイプ宣言
  +----------------------------------------------------------------+*/
#endif	// __FILL_MGR_HPP__
